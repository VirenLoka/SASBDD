# DiffSBDD Requirements
# Versions have been relaxed to ensure compatibility with modern Python environments.

# ── Core Deep Learning ────────────────────────────────────────────────────────
torch
pytorch-lightning>=1.8.4
torchmetrics>=1.4.2

# ── PyTorch Geometric ecosystem ───────────────────────────────────────────────
# Note: If pip fails to build torch-scatter, you may need to install it from PyG 
# matching your specific torch/cuda versions:
# pip install torch-scatter -f https://data.pyg.org/whl/torch-<version>+<cuda>.html
torch-scatter
torch-geometric

# ── Chemistry / Bioinformatics ────────────────────────────────────────────────
rdkit
biopython>=1.79
openbabel-wheel       # Used instead of 'openbabel' to avoid complex source builds via pip

# ── Scientific Computing ──────────────────────────────────────────────────────
numpy<2.0.0           # Capped below 2.0 to prevent PyTorch/SciPy compatibility issues
scipy
networkx
pandas

# ── Experiment Tracking ───────────────────────────────────────────────────────
wandb

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib
seaborn
imageio

# ── Data Storage ─────────────────────────────────────────────────────────────
lmdb

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm
pyyaml
protobuf
