# Moved to configs/stage1_surrogate.yaml as part of the project-wide config
# refactor.  Kept as an alias so existing commands and in-flight runs keep
# working; the schema is unchanged.
defaults: [../../configs/stage1_surrogate.yaml]
